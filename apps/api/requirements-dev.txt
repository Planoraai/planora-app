# =====================================================================
# Development-only dependencies
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
# =====================================================================

-r requirements.txt

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
respx==0.21.1                   # httpx mocking
freezegun==1.5.1

# --- Linting / typing / formatting ---
ruff==0.6.9
mypy==1.11.2
types-redis==4.6.0.20240903

# --- Dev workflow ---
pre-commit==3.8.0
invoke==2.2.0                   # cross-platform task runner (tasks.py)
