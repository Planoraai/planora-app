NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# =====================================================================
# AI Travel Planner — Frontend (Next.js) local environment
# Copy to: apps/web/.env.local
# Phase 8: wire these into fetch() / Server-Sent Events client.
# =====================================================================

# Base URL of the FastAPI backend (see apps/api/)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase (frontend-safe values only)
# Use the project's Publishable key here (never the Secret/service_role key)
NEXT_PUBLIC_SUPABASE_URL=https://zbrmxtvhiaeizlqwekac.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_yXam_w2FL8bqlAQ6CAAdNQ_bMYq_7Gs
