"""Versioned prompt templates.

Each prompt file should declare a `VERSION` constant so we can correlate
LLM output with the exact template that produced it (see docs/implementationPlan.md
section 13 - Cross-Phase Workstreams).
"""

from __future__ import annotations
